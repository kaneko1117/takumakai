import { Menu } from "./components/menu";

export const Footer = () => {
  return (
    <footer className="fixed bottom-0 max-w-[428px] w-full flex justify-between items-center">
      <Menu />
    </footer>
  );
};
