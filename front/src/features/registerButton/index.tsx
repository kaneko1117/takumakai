import { Button } from "@/components/shadcn/button";

export const RegisterButton = () => {
  return (
    <Button variant="default" className="font-bold cursor-pointer">
      入会
    </Button>
  );
};
